<project name="CAB302-MazeDesignTool">
    <property name="src" location="src"/>
    <property name="build" location="build"/>
    <property name="dist" location="dist"/>
    <property name="lib" location="lib"/>

    <path id="class.path">
        <fileset dir="lib">
            <include name="**/*.jar" />
        </fileset>
    </path>

    <target name="init">
        <!-- Create the time stamp -->
        <tstamp/>
        <!-- Create the build directory structure used by compile -->
        <mkdir dir="${build}"/>
    </target>

    <target name="compile" depends="init"
            description="compile the source">
        <!-- Compile the Java code from ${src} into ${build} -->
        <javac srcdir="${src}" destdir="${build}">
            <classpath refid="class.path"/>
        </javac>
    </target>

    <target name="dist" depends="compile" description="generate the distribution">
        <copyfile src="lib/mariadb-java-client-3.0.5.jar" dest="${dist}/mariadb-java-client-3.0.5.jar"/>
        <copyfile src="db.props" dest="${dist}/db.props"/>
        <copyfile src="launch.bat" dest="${dist}/launch.bat"/>
        <copyfile src="README.md" dest="${dist}/README.md"/>

        <!-- Put everything in ${build} into the MyProject-${DSTAMP}.jar file -->
        <jar jarfile="${dist}/MazeDesignTool.jar" basedir="${build}">
            <manifest>
                <!-- Who is building this jar? -->
                <attribute name="Built-By" value="${user.name}"/>
                <!-- The application entry point -->
                <attribute name="Main-Class" value="GUI.PageLogin"/>
            </manifest>
        </jar>
    </target>

    <target name="clean" description="clean up">
        <!-- Delete the ${build}, ${dist}, and any generated images in export directory trees -->
        <delete dir="${build}"/>
        <delete dir="${dist}"/>
        <delete dir="export"/>
    </target>
</project>